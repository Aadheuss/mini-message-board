<!DOCTYPE html>
<html lang="en">
  <%-include('./head', {title: title}) %>
  <body>
    <%-include("./nav") %>
    <% if (locals.message){ %>
    <p><%= message.username %></p>
    <p><%= message.message %></p>
    <p><%= message.added %></p>
    <% } else{ %>  
    <p>No message found</>
    <% } %>
  </body>
</html>